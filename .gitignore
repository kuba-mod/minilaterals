.env
.venv/
__pycache__/
*.pyc
*.db
*.db-shm
*.db-wal
.DS_Store

# Local Claude Code settings (machine-specific)
.claude/

# Wrangler local build/cache
.wrangler/

# Rendered site — a build artifact. Cloudflare renders it from source on every
# push (scripts/cf-build.sh) and serves it; it is never committed.
/docs/

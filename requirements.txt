# Weimar Triangle tracker — install with: pip install -r requirements.txt

# Ingestion
feedparser>=6.0
requests>=2.31
beautifulsoup4>=4.12
lxml>=5.0
pyyaml>=6.0

# Enrichment — provider is Ollama (local) or Anthropic (API key required)
anthropic>=0.40
openai>=1.0       # used as the Ollama client via its OpenAI-compatible endpoint

# Rendering
jinja2>=3.1
altair>=5.0
pandas>=2.0
